{
  "name": "med-vito-db-migrations",
  "private": true,
  "description": "Схема БД и миграции Prisma; HTTP API — каталог api-go/",
  "scripts": {
    "prisma:migrate": "prisma migrate deploy",
    "prisma:migrate:dev": "prisma migrate dev",
    "prisma:generate": "prisma generate"
  },
  "devDependencies": {
    "dotenv": "^17.2.3",
    "prisma": "^7.2.0"
  }
}
